// Secrets.swift.template — committed to version control.
// Copy this file to Secrets.swift and replace the placeholder with a real key.
// Secrets.swift is listed in .gitignore and must never be committed.
enum Secrets {
    static let googlePlacesAPIKey = "YOUR_GOOGLE_PLACES_API_KEY_HERE"
}
